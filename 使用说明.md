# 研词助手 · 考研英语一词汇工作台

一个本地运行的考研英语一词汇识别、翻译与记忆工具。上传资料即可把英文变成可点击的单词（扫描版 PDF 会自动 OCR 识别），单击查看**适用于考研英语一的释义**并标黄，长按拖拽识别词组，在右侧面板一键收录进记忆区，随时导出 Word / PDF 背诵。

---

## 一、快速开始

1. **双击 `启动.bat`**（需要本机已安装 Node.js 或 Python 3；若没有，请先安装 [Node.js](https://nodejs.org/zh-cn)）
2. 浏览器会自动打开 `http://127.0.0.1:8765/`
3. 在「工作区」点击 **📂 上传资料**，或直接把 PDF / Word / TXT 拖进阅读区

> 为什么不直接双击 HTML 打开？因为 PDF 解析、导出字体、在线词典都需要本地服务支持，启动脚本已替你完成这些。

## 二、三大功能区

### 1. 工作区 —— 识别上传资料中的单词
| 操作 | 效果 |
| --- | --- |
| **单击**英文单词 | 右侧面板弹出**原形 + 考研释义 + 词群释义**，同时把单词**标黄**；点派生形式（如 moves）时**原形排在词群第一位**，可直接查看和 📥 收录 |
| **按住左键约 0.4 秒后拖拽** | 文本模式框选多个单词识别**词组**；扫描模式识别框内同样支持长按拖选词组 |
| 右侧释义面板中 **📥 收录到记忆区** | 把单词/词组 + 释义录入记忆区 |
| 右侧面板中 **取消标黄 / 标黄** | 手动控制标黄状态 |
| 面板中 **词典中打开** | 跳到词典页继续查询 |
| 侧栏 **缩放** | 阅读区大小 12–40px（扫描模式为页面显示宽度） |
| **左右侧栏手柄** | 拖动调宽（内容随宽度自动缩放）、点箭头收起/展开；宽度与显示状态自动保存 |
| 勾选 **文本选择模式** | 临时恢复鼠标划选，方便复制原文 |
| **清除全部标黄** | 清除当前文档的标黄记录 |
| **扫描模式**：按住左键拖拽框选区域 | 识别该区域文字（图片 OCR，引擎内置离线运行），结果以「已识别」覆盖框显示在原位置 |
| 识别框中点单词 / 长按拖选 / 📥 收录全部 | 单击单词查释义并标黄；长按拖选可框选识别框内的词组；「收录全部」把整段文字 + 逐词释义录入记忆区 |

支持 **PDF / DOCX / DOC / TXT / MD**，单文件最大 **1GB**，可多选、可拖拽、多文档切换。**超过 300 页的 PDF** 在解析前可输入页码范围（如 `120-180`）只打开需要的部分。**扫描版 PDF（无文字层）自动进入扫描模式**：原书逐页以高清图片显示（滚动加载、自动释放内存），**按住左键拖拽框选任意文字区域即自动 OCR 识别**，识别文字以覆盖框显示在原位置，框内单词可直接点击查释义、标黄、收录。

### 2. 记忆区 —— 收录的单词与释义
- 按**收录顺序**排布，一行一条（单词 + 释义），带序号
- **直接点击文字即可编辑**（单词和释义都可改），自动保存
- 支持**搜索**、**缩放**（12–32px，只缩放单词/释义列表区域，工具栏与滑条保持固定、拖动不跑偏）、单条**删除**、一键**清空**
- **🔀 词群整理**：把同一词族的单词（词性变化/派生/相关词，如 generate→generator→generation）整理到一起：
  - **指定单词整理**：输入一个单词 → 自动找出记忆区中它的词群成员 → 词群**插到所选单词之后**（所选单词保持原位不动），也可改为**插到指定序号之后**
  - **一键整理**：对全部范围或指定序号范围，自动把所有词群分组并相邻排列（词群内按单词长度排序，词干最短的排最前）
- **⬇ 导出 Word**：生成 `.docx`（标题 + 序号 + 单词 + 释义，顺序与记忆区一致）
- **⬇ 导出 PDF**：生成 A4 `.pdf`（内置 Noto 中文字体，顺序与记忆区一致）
- 数据保存在浏览器本地（localStorage），重启不丢失

### 3. 词典 —— 英中双向自由查询
- 输入**英文**（如 `economy`）查中文释义；输入**中文**（如 `经济`）反查英文单词
- 顶栏「**词库来源**」可切换：
  - **内置考研词库（离线）**：考研英语大纲 5530 词（含词频与分类）+ 红宝书补充词，共 7600+ 词条，无需联网
  - **有道词典（在线）**：联网查询，英中 / 中英均可，工作区右侧面板也会附带在线释义
- 查询结果一键 **📥 收录到记忆区**
- 支持模糊命中：不规则变化（is→be、went→go）、复数/时态（candidates→candidate、watching→watch）、连字符（well-being→wellbeing）等自动还原

## 三、内置词库说明

- **主体**：[exam-data/NETEMVocabulary](https://github.com/exam-data/NETEMVocabulary) 的考研英语大纲词汇词频排序表（5530 词，含词频、分类、拼写变体），即教育部考试中心《全国硕士研究生招生考试英语（一/二）考试大纲》词汇表
- **释义增强**：qwerty-learner 的《2025 考研英语红宝书》词库与 2024 考研英语词库（含词性与多义项）、maimemo 红宝书导出词表
- 生成的词库文件：`www/js/wordlist.js`（7646 词条，可自行用脚本重新生成）

## 四、常见问题

- **端口被占用**：关闭占用 8765 端口的程序，或编辑 `启动.bat` / `server.js` 中的端口号
- **在线词典连不上**：有道接口需要联网；不影响离线词库使用
- **扫描版 PDF（无文字层）**：自动以「扫描模式」打开——全书逐页以图片显示，无需整本识别。想看哪段文字，就**按住左键约 0.4 秒后拖拽框选那一段**，软件即时识别（OCR 引擎内置、完全离线、无需联网），识别文字以「已识别」覆盖框显示在原位置，框内单词可点击查释义、标黄，「收录全部」可把整段收入记忆区。框选区域越贴合文字、版面越清晰，识别越准；用「缩放」放大页面后再框选小字效果更好。
- **大文件解析慢**：1GB 以内的文件都能上传；超过 300MB 会有提示，超过 300 页的 PDF 可只解析页码范围，单次最多 2000 页
- **旧版 .doc 解析失败**：先用 WPS / Word 另存为 `.docx`
- **导出的 Word 乱码**：一般不会；如遇问题请用新版 WPS / Office 打开
- **想换电脑使用**：记忆区数据保存在浏览器 localStorage（按域名），如需迁移可先导出 Word/PDF 再手动录入，或复制浏览器配置
- **只用离线功能**：把顶栏词库来源选为「内置考研词库（离线）」，断网也能正常使用（启动仍需本地服务）

## 五、目录结构

```
考研英语一词汇助手/
├── 启动.bat            # 一键启动（Windows）
├── server.js           # 本地服务（Node.js）
├── server.py           # 本地服务（Python 备用）
├── 测试资料.txt        # 示例英文材料
└── www/
    ├── index.html
    ├── css/style.css
    ├── js/             # app / workspace / memory / dictionary / export / wordlist
    ├── libs/           # pdf.js、mammoth、pdf-lib、docx
    └── fonts/          # Noto Sans CJK SC 中文字体（PDF 导出用）
```
