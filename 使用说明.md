# 研词助手 · 考研英语一词汇工作台

一个本地运行的考研英语一词汇「查看 · 识别 · 翻译 · 记忆」工具。所有文件（PDF / Word / TXT / 图片）都以**查看器方式**打开、按原本排版展示；**按住左键拖拽框选任意区域即识别文字**，识别结果中单词可点击查**考研英语一释义**并标黄、收录；内置翻译区与记忆区（导出/导入 Word、PDF，词群整理）。

---

## 一、快速开始

1. **双击 `启动.bat`**（需要本机已安装 Node.js 或 Python 3；若没有，请先安装 [Node.js](https://nodejs.org/zh-cn)）
2. 浏览器会自动打开 `http://127.0.0.1:8765/`
3. 在「工作区」点击 **📂 上传资料**，或直接把文件拖进阅读区

> 为什么不直接双击 HTML 打开？因为 PDF 解析、导出字体、在线词典/翻译都需要本地服务支持，启动脚本已替你完成这些。

## 二、三大功能区

### 1. 工作区 —— 查看 + 框选识别
| 操作 | 效果 |
| --- | --- |
| 上传任意文件 | 以查看器打开：PDF 逐页渲染、Word/TXT 转为排版画布、多张照片按顺序拼接成一张长图（间距可调），展示内容原本的样子 |
| **按住左键约 0.4 秒后拖拽框选** | 识别框内文字（本地 OCR，离线运行），结果以「已识别」覆盖框显示在原位置 |
| 识别框内单击单词 | 右侧面板显示原形 + 考研释义 + 词群释义，并标黄 |
| 识别框内长按拖选 | 框选多个单词识别词组 |
| 右侧面板 **📥 收录** / 识别框 **收录全部** | 录入记忆区 |
| 侧栏 **缩放 / 左右栏拖宽与收起** | 页面显示宽度 12–40px；侧栏可拖宽（内容随之缩放）、可收起 |
| **图片间距**（多图时） | 0–80px 灰条分隔，默认 8px，拖动实时重拼 |
| 超大 PDF | 超过 300 页可输入页码范围；渲染队列+任务取消+滚动回收，快速拖进度条不卡 |

支持 **PDF / DOCX / DOC / TXT / MD / 图片（jpg/png/webp/bmp/gif）**，单文件最大 1GB，可多选、可拖拽、多文档切换。

### 2. 记忆区 —— 收录的单词与释义
- 按**收录顺序**排布，一行一条（单词 + 释义），带序号；**动词自动展示时态变化、形容词展示比较级/最高级**
- **直接点击文字即可编辑**（单词和释义都可改），自动保存
- 支持**搜索**、**缩放**（只缩放列表区域，滑条不跑偏）、单条**删除**、一键**清空**
- **🔀 词群整理**：指定单词整理（词群插到所选单词之后/指定序号之后）/ 一键整理（范围内词群自动相邻分组）
- **导出 / 导入 Word、PDF**：格式互通，顺序与记忆区一致
- 数据保存在浏览器本地（localStorage），重启不丢失

### 3. 翻译区 —— 查词 + 翻译
- **单个英文单词**：查考研释义 + 音标 + 词形变化 + 词群释义（成员可点击切换查询、一键收录）+ 翻译
- **长段英文 / 中文**：自动整段翻译——优先**在线翻译**（有道/QQ 多源自动切换），网络不可用时自动切换**离线对照翻译**（英文逐词对照、中文逐句反查），永远有结果
- 短中文（≤6 字）：离线反查对应英文单词 + 翻译
- 支持模糊命中：不规则变化（is→be）、复数/时态（candidates→candidate）、连字符（well-being→wellbeing）等自动还原
- 顶栏「词库来源」：控制工作区在线翻译开关与在线词典；离线词库始终可用

## 三、内置词库说明

- **主体**：[exam-data/NETEMVocabulary](https://github.com/exam-data/NETEMVocabulary) 的考研英语大纲词汇词频排序表（5530 词，含词频、分类、拼写变体）
- **释义增强**：qwerty-learner 的《2025 考研英语红宝书》词库与 2024 考研英语词库、maimemo 红宝书导出词表
- 词库文件：`www/js/wordlist.js`（7646 词条）

## 四、常见问题

- **端口被占用**：关闭占用 8765 端口的程序，或修改 `server.js` / `启动.bat` 中的端口号
- **在线翻译不可用**：自动切换离线对照翻译；在线翻译需联网且依赖第三方接口可用性
- **识别效果**：框选区域越贴合文字、版面越清晰识别越准；放大页面后再框选小字效果更好
- **旧版 .doc 解析失败**：先用 WPS / Word 另存为 `.docx`
- **想换电脑使用**：记忆区数据保存在浏览器 localStorage，重要数据先导出 Word/PDF 备份
- **只用离线功能**：词库、OCR、字体全部内置，断网可用全部核心功能（启动仍需本地服务）

## 五、目录结构

```
考研英语一词汇助手/
├── 启动.bat            # 一键启动（Windows）
├── server.js           # 本地服务（Node.js）：静态文件 + 在线词典/翻译多源代理
├── server.py           # 本地服务（Python 备用）
├── 测试资料.txt        # 示例英文材料
└── www/
    ├── index.html
    ├── css/style.css
    ├── js/             # app / workspace / memory / dictionary / export / wordlist
    ├── libs/           # pdf.js、mammoth、pdf-lib、docx、tesseract.js 等
    ├── fonts/          # Noto Sans CJK 中文字体（PDF 导出用）
    └── tessdata/       # 中英文 OCR 识别模型
```
