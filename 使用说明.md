# 研词助手 · 考研英语一词汇工作台

一个本地运行的考研英语一词汇「查看 · 识别 · 记忆」工具。所有文件（PDF / Word / TXT / 图片）都以**查看器方式**打开、按原本排版展示；**按住左键拖拽框选任意区域即识别文字**，识别结果中单词可点击查**考研英语一释义**并标黄、收录；内置词典区与记忆区（导出/导入 Word、PDF，词群整理）。

---

## 一、快速开始

### 方式一：安装版（推荐，安装后自动创建桌面快捷方式）

1. 从 Releases 下载 `YanCi-KaoYan-Vocab-Workbench-*-setup.exe`，双击安装（默认中文界面，可自选安装目录）
2. 安装完成后**桌面自动出现「研词助手」快捷方式**，开始菜单同步创建
3. 双击快捷方式启动；卸载请到「设置 → 应用」或运行安装目录下的卸载程序

### 方式二：绿色便携版

下载 `*-portable.exe` 双击即用：免安装、不写注册表、不创建快捷方式，可放 U 盘随身携带。

### 方式三：源码运行（开发者）

需要 Node.js 18+ 或 Python 3.8+：双击 `启动.bat`，浏览器自动打开 `http://127.0.0.1:8765/`；桌面版源码运行：`desktop` 目录下 `npm install && npm start`。

> 安装版/便携版已内置全部运行环境、词库、字体与 OCR 引擎，无需安装 Node / Python，断网可用（仅「在线词典」需联网）。

## 二、三大功能区

### 1. 工作区 —— 查看 + 框选识别
| 操作 | 效果 |
| --- | --- |
| 上传任意文件 | 以查看器打开：PDF 逐页渲染、Word/TXT 转为排版画布、多张照片按顺序拼接成一张长图（间距可调），展示内容原本的样子 |
| **按住左键约 0.4 秒后拖拽框选** | 识别框内文字（本地 OCR，离线运行），结果以「已识别」覆盖框显示在原位置 |
| 识别框内单击单词 | 右侧面板显示原形 + 考研释义 + 词群释义，并标黄 |
| 识别框内长按拖选 | 框选多个单词识别词组 |
| 右侧面板 **📥 收录** / 识别框 **收录全部** | 录入记忆区 |
| 侧栏 **缩放 / 左右栏拖宽与收起** | 页面显示宽度 12–40px；侧栏可拖宽（内容随之缩放）、可收起 |
| **图片间距**（多图时） | 0–80px 灰条分隔，默认 8px，拖动实时重拼 |
| 超大 PDF | 超过 300 页可输入页码范围；**后台自动生成速览缩略图**：拖滚动条到哪里就立即显示该页（低清速览），随后自动替换为高清；最近页优先渲染、远处渲染任务自动取消 |

支持 **PDF / DOCX / DOC / TXT / MD / 图片（jpg/png/webp/bmp/gif）**，单文件最大 1GB，可多选、可拖拽、多文档切换。
大文件（数百 MB）也能流畅打开：PDF 按磁盘路径解析、文本流式读取，超大内容自动截断前 300 万字符。

### 2. 记忆区 —— 收录的单词与释义
- 按**收录顺序**排布，一行一条（单词 + 释义），带序号；**动词自动展示时态变化、形容词展示比较级/最高级**
- **直接点击文字即可编辑**（单词和释义都可改），自动保存
- 支持**搜索**、**缩放**（只缩放列表区域，滑条不跑偏）、单条**删除**、一键**清空**
- **🔀 词群整理**：指定单词整理（词群插到所选单词之后/指定序号之后）/ 一键整理（范围内词群自动相邻分组）
- **导出 / 导入 Word、PDF**：格式互通，顺序与记忆区一致
- 数据保存在应用本地（localStorage），重启不丢失

### 3. 词典 —— 英中双向查询
- 输入**英文**（如 `economy`）查中文释义；输入**中文**（如 `经济`）反查英文单词
- **查询卡片展示查询词本身**：查 `amazing` 显示/收录的就是 amazing，原形 amaze 作为「原形」标签与词群首位辅助展示
- **词形变化展示**：动词展示时态变化（三单 / 过去式 / 过去分词 / 现在分词），形容词展示比较级与最高级（不附释义）；记忆区收录的动词/形容词同样显示
- **词群释义**：卡片附带词族成员（词性变化/派生/相关词），点成员行即切换查询、点 📥 直接收录
- 顶栏「**词库来源**」可切换：
  - **内置考研词库（离线）**：考研英语大纲 5530 词（含词频与分类）+ 红宝书补充词，共 7600+ 词条，无需联网
  - **有道词典（在线）**：联网查询，英中 / 中英均可（自动兜底必应），工作区右侧面板也会附带在线释义
- 查询结果一键 **📥 收录到记忆区**
- 支持模糊命中：不规则变化（is→be）、复数/时态（candidates→candidate）、连字符（well-being→wellbeing）等自动还原

## 三、内置词库说明

- **主体**：[exam-data/NETEMVocabulary](https://github.com/exam-data/NETEMVocabulary) 的考研英语大纲词汇词频排序表（5530 词，含词频、分类、拼写变体）
- **释义增强**：qwerty-learner 的《2025 考研英语红宝书》词库与 2024 考研英语词库、maimemo 红宝书导出词表
- 词库文件：`www/js/wordlist.js`（7646 词条）

## 四、常见问题

- **首次运行安全软件提示**：选择「允许」——程序为本地应用，只在 127.0.0.1 本地回环地址监听，不会联网上传你的任何数据
- **端口被占用**（仅源码运行模式）：关闭占用 8765 端口的程序，或修改 `server.js` / `启动.bat` 中的端口号
- **在线词典连不上**：有道接口需要联网（自动兜底必应）；不影响离线词库使用
- **识别效果**：框选区域越贴合文字、版面越清晰识别越准；放大页面后再框选小字效果更好
- **旧版 .doc 解析失败**：先用 WPS / Word 另存为 `.docx`
- **想换电脑使用**：记忆区数据保存在应用本地，重要数据先导出 Word/PDF 备份
- **如何卸载安装版**：Windows「设置 → 应用」中卸载「研词助手」，或运行安装目录下的卸载程序
- **只用离线功能**：词库、OCR、字体全部内置，断网可用全部核心功能

## 五、目录结构

```
YanWords/
├── desktop/                # 桌面版：Electron 外壳 + MuPDF 桥接 + 打包脚本
│   ├── main.js / preload.js
│   ├── scripts/prepack.js
│   └── package.json        # NSIS 安装版（自动桌面快捷方式）+ 便携版
├── 启动.bat                # 浏览器版一键启动（Windows）
├── server.js               # 本地服务（Node.js）：静态文件 + 在线词典多源代理
├── server.py               # 本地服务（Python 备用）
├── 测试资料.txt            # 示例英文材料
└── www/
    ├── index.html
    ├── css/style.css
    ├── js/                 # app / workspace / memory / dictionary / export / wordlist
    ├── libs/               # pdf.js、mammoth、pdf-lib、docx、tesseract.js 等（按需加载）
    ├── fonts/              # Noto Sans CJK 中文字体（PDF 导出用）
    └── tessdata/           # 中英文 OCR 识别模型
```

## 六、更新记录

- **v1.2.0 · 性能与内存大幅优化**：大 PDF 按磁盘路径直接解析（不再整文件读入内存，数百 MB 文件秒开不卡死）；大文本流式读取；文本查看器画布按视口渲染与回收；PDF 速览缩略图缓存封顶、围绕视口滑窗生成；六大解析组件按需加载，启动更快、常驻内存更低；修复切换文档滚动位置不归零等问题。新增 **Windows 安装版（安装后自动创建桌面快捷方式）**。
- **v1.1.x · 独立桌面版**：Electron 外壳 + MuPDF 原生渲染（打开快 5–8 倍）；PDF 速览缩略图「拖到哪显示到哪」；高清 1.5 倍渲染；应用内模态框；OCR worker 适配。
- **v1.0.0 · 浏览器版**：统一查看器 + 框选 OCR + 考研词典 + 记忆区。
